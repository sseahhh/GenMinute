name: genminute
channels:
  - conda-forge
  - defaults
dependencies:
  # Python (정확한 버전 명시로 재현성 보장)
  - python=3.11.13

  # Conda 기본 패키지 (크로스 플랫폼)
  - pip
  - sqlite
  - openssl
  - ca-certificates

  # Pip 패키지 (핵심만)
  - pip:
      # 웹 프레임워크
      - flask==3.1.2
      - werkzeug==3.1.3
      - jinja2==3.1.6
      - itsdangerous==2.2.0
      - click==8.3.0
      - blinker==1.9.0

      # Google AI & Firebase
      - google-genai==1.47.0
      - google-auth==2.42.1
      - firebase-admin==7.1.0
      - googleapis-common-protos==1.71.0
      - grpcio==1.76.0
      - protobuf==6.33.0

      # Vector DB & RAG (최신 1.0.x로 업그레이드!)
      - chromadb==1.3.0
      - langchain==1.0.5
      - langchain-core==1.0.4
      - langchain-chroma==1.0.0
      - langchain-classic==1.0.0
      - langchain-community==0.4.1
      - langchain-openai==1.0.2
      - langchain-text-splitters==1.0.0

      # OpenAI
      - openai==2.6.1
      - tiktoken==0.12.0

      # 데이터 처리
      - pandas==2.3.3
      - numpy==2.3.4

      # 유틸리티
      - python-dotenv==1.2.1
      - requests==2.32.5
      - pyyaml==6.0.3
      - python-dateutil==2.9.0

      # 마인드맵 (누락되어 있었음!)
      - pydot==3.0.2
      - graphviz==0.20.3

      # 기타 필수
      - pydantic==2.12.3
      - pydantic-core==2.41.4
      - sqlalchemy==2.0.44
      - bcrypt==5.0.0
      - cryptography==46.0.3
      - certifi==2025.10.5
      - urllib3==2.3.0
